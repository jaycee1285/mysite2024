---
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import Link from "@components/Link.astro";
import { SITE, HOME, SOCIALS } from "@consts";
---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <h3>
    Your cure for
    </h3>
    <h1
    class="relative w-[max-content] text-right font-serif text-3xl text-color-accent dark:text-color-accent justify-end before:absolute before:inset-0 before:animate-typewriter before:bg-bg-light dark:before:bg-bg-dark after:absolute after:inset-0 after:w-[0.125em] after:animate-caret after:bg-bg-dark after:dark:bg-bg-light">
    Blank Page Syndrome
</h1>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
<p>When your technical content has no SME, no ICP, and barely a usable brief, someone still has to write it. </p>

<p>Hi. </p>

<p>That’s me. I write for RF labs and compliance-heavy manufacturers, or marketing teams already underwater. Whether the content sells $50K EMC testing contracts or convinces a buyer to drive across county lines to a dealership, it’s built to serve decisions — not vanity metrics. </p>

<p>Most of my work starts at the hard end: botched copy, spec-heavy source material, or service lines no one can explain cleanly. I work around silence, under time pressure, and inside subject matter no one has time to simplify. </p>

<p>If your marketing environment is stretched thin and your content still needs to hit — I’ll take it from here. </p>

<p> → See selected <Link href="/writing">
          work
        </Link></p>
<p> → Download <Link href="/JohnMCurran.pdf">
          resume
        </Link></p>
<p> → <Link href="mailto:curran.john.m@gmail.com">
          Reach Out
        </Link> </p>

<!-- <p class="animate">
            I am a minimal, seo friendly, accessible portfolio and blog for developers. 
            I am an even more lightweight version of my popular theme
            <Link href="https://github.com/markhorn-dev/astro-sphere" aria-label="Astro Sphere on Github" external>
              Astro Sphere
            </Link>.
            I have a lighthouse scores of 100 across the board for performance, 
            accesibility, best practices and SEO.
          </p>
          <p class="animate">
            I come packed with full type safety, a sitemap, an rss feed, markdown 
            and mdx support (use components in your markdown) through astro integrations.
            I am styled with tailwind and come preconfigured with light, dark and 
            system/os theme preferences out of the box.
          </p>
          <p class="animate">
            Visit
            <Link href="https://github.com/markhorn-dev/astro-nano" aria-label="Astro Nano on Github" external>
              Astro Nano on Github
            </Link>
            to fork the repository, read the docs or one-click deploy on Vercel or Netlify. 
          </p>
        </article>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Latest posts
          </h5>
          <Link href="/blog">
            See all posts
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold text-black dark:text-white">
                {entry.data.company}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Recent projects
          </h5>
          <Link href="/projects">
            See all projects
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} />
            </li>
          ))}
        </ul>
      </section>
      <section class="animate space-y-4">
        <h5 class="text-center font-serif text-black dark:text-white">
          Let's Connect
        </h5>
        <article>
          <p>
            Reach out on LinkedIn or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2 justify-center">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
        </ul>
      </section> -->
    </div>
  </Container>
</PageLayout>
