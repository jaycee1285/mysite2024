---
import Container from "@components/Container.astro";
import PageLayout from "@layouts/PageLayout.astro";
import Link from "@components/Link.astro";
import { SITE, HOME, SOCIALS } from "@consts";
---

<PageLayout title={HOME.TITLE} description={HOME.DESCRIPTION}>
  <Container>
    <h3>
    Your cure for
    </h3>
    <h1
    class="relative w-[max-content] text-right font-mono text-3xl text-[#335669] justify-end before:absolute before:inset-0 before:animate-typewriter before:bg-stone-100 dark:before:bg-stone-900 after:absolute after:inset-0 after:w-[0.125em] after:animate-caret after:bg-black">
    Blank Page Syndrome
</h1>
    <div class="space-y-16">
      <section>
        <article class="space-y-4">
<p> Hi. My name is <strong>John Curran</strong> and I <s>clutter screens for a living</s> write copy and create marketing strategies that bring clients and customers to your business.
</p>

<p>I've been lucky enough to have done work for companies you might have heard of... </p>

<p>Toyota. eBay. SAE. But everyone has a feathers in their cap, and mine aren't TV ads on Super Bowl Sunday.</p>

<p> I do the grunt work of ensuring brochures are accurate, safety protocols are clear, things like that. Case studies, blog posts. You know: B2B.</p>

<p> I've done it for:</p>
<p class="text-center"> manufacturers,</p>
  <p class="text-center">  industrial suppliers, </p>
    <p class="text-center">SaaSes of all shapes and sizes, </p>
      <p>and I'd love to work with you next. Check out my <Link  href="/work"> background </Link> or my <Link href="/blog">articles</Link> and <Link href="/casestudies">case studies</Link>. Then,</p>


<!-- <p class="animate">
            I am a minimal, seo friendly, accessible portfolio and blog for developers. 
            I am an even more lightweight version of my popular theme
            <Link href="https://github.com/markhorn-dev/astro-sphere" aria-label="Astro Sphere on Github" external>
              Astro Sphere
            </Link>.
            I have a lighthouse scores of 100 across the board for performance, 
            accesibility, best practices and SEO.
          </p>
          <p class="animate">
            I come packed with full type safety, a sitemap, an rss feed, markdown 
            and mdx support (use components in your markdown) through astro integrations.
            I am styled with tailwind and come preconfigured with light, dark and 
            system/os theme preferences out of the box.
          </p>
          <p class="animate">
            Visit
            <Link href="https://github.com/markhorn-dev/astro-nano" aria-label="Astro Nano on Github" external>
              Astro Nano on Github
            </Link>
            to fork the repository, read the docs or one-click deploy on Vercel or Netlify. 
          </p>
        </article>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Latest posts
          </h5>
          <Link href="/blog">
            See all posts
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {blog.map(post => (
            <li>
              <ArrowCard entry={post} />
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Work Experience
          </h5>
          <Link href="/work">
            See all work
          </Link>
        </div>
        <ul class="flex flex-col space-y-4">
          {work.map(entry => (
            <li>
              <div class="text-sm opacity-75">
                {dateRange(entry.data.dateStart, entry.data.dateEnd)}
              </div>
              <div class="font-semibold text-black dark:text-white">
                {entry.data.company}
              </div>
              <div class="text-sm opacity-75">
                {entry.data.role}
              </div>
              <article>
                <entry.Content />
              </article>
            </li>
          ))}
        </ul>
      </section>

      <section class="animate space-y-6">
        <div class="flex flex-wrap gap-y-2 items-center justify-between">
          <h5 class="font-semibold text-black dark:text-white">
            Recent projects
          </h5>
          <Link href="/projects">
            See all projects
          </Link>
        </div>
        <ul class="flex flex-col gap-4">
          {projects.map(project => (
            <li>
              <ArrowCard entry={project} />
            </li>
          ))}
        </ul>
      </section>
-->
      <section class="animate space-y-4">
        <h5 class="text-center font-semibold text-black dark:text-white">
          Let's Connect
        </h5>
        <article>
          <p>
            Reach out on LinkedIn or send me an email.
          </p>
        </article>
        <ul class="flex flex-wrap gap-2 justify-center">
          {SOCIALS.map(SOCIAL => (
            <li class="flex gap-x-2 text-nowrap">
              <Link href={SOCIAL.HREF} external aria-label={`${SITE.NAME} on ${SOCIAL.NAME}`}>
                {SOCIAL.NAME}
              </Link>
              {"/"}
            </li>
          ))}
          <li class="line-clamp-1">
            <Link href={`mailto:${SITE.EMAIL}`} aria-label={`Email ${SITE.NAME}`}>
              {SITE.EMAIL}
            </Link>
          </li>
        </ul>
      </section>
    </div>
  </Container>
</PageLayout>
